{
  "name": "desa-pancanegara-modern",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod",
    "hash-password": "node scripts/hash-password.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^23.9.0"
  }
}